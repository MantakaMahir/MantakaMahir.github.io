import type { Metadata } from "next";
import Link from "next/link";
import { VoiceTextFooter, VoiceTextHeader } from "../VoiceTextShell";

export const metadata: Metadata = { title: "VoiceText Privacy Policy", description: "VoiceText Privacy Policy, effective August 9, 2026.", alternates: { canonical: "/voicetext/privacy" } };

export default function PrivacyPage() {
  return <div className="min-h-screen bg-[#f8f5ee] text-[#183d38]"><VoiceTextHeader /><main className="mx-auto max-w-3xl px-5 pb-20 pt-10 sm:px-8 sm:pt-16"><Link href="/voicetext" className="text-sm text-[#356258] underline decoration-[#e5a85b] underline-offset-4">← Back to VoiceText</Link><header className="mt-10 border-b border-[#d8d5cc] pb-10"><p className="text-xs font-bold uppercase tracking-[.18em] text-[#d17e38]">Privacy · effective Aug 9, 2026</p><h1 className="mt-5 text-4xl font-semibold tracking-[-.045em] sm:text-6xl">VoiceText Privacy Policy</h1><p className="mt-5 text-[#68736c]">Last updated Aug 9, 2026</p></header><article className="prose prose-lg mt-10 max-w-none text-[#53615b] prose-headings:font-semibold prose-headings:tracking-tight prose-headings:text-[#183d38] prose-a:text-[#356258] prose-strong:text-[#183d38]"><p>VoiceText is a Manifest V3 extension for Chrome and Edge. It adds an explicit Transcribe control to individual voice messages on WhatsApp Web and Messenger. This policy explains what the extension processes and what it does not.</p><h2>What VoiceText processes</h2><p>VoiceText processes audio only after you explicitly choose a specific voice message and enable active-tab capture from the extension action where required. It captures only audio from the active target tab; it never captures microphone or desktop audio.</p><p>Audio travels from the extension to the separately-run local VoiceText backend configured at <code>127.0.0.1:8787</code>, and then to the Gemini Developer API for transcription. The Gemini API key is kept in the backend and is not placed in the extension.</p><h2>Provider data processing</h2><p>The Gemini Developer API receives request-scoped audio and the transcription request needed to provide the feature. VoiceText does not control the provider’s independent processing practices. Review the provider’s applicable terms and privacy documentation for details about its handling of API data.</p><h2>Storage and retention</h2><p>Raw audio is request-scoped only and is not persisted by VoiceText. VoiceText may keep a local, transcript-only cache keyed by SHA-256. The extension also stores your language preference and privacy notice acknowledgement locally. VoiceText does not send analytics or tracking data.</p><h2>Information VoiceText does not log</h2><p>VoiceText does not log contact text, message text, voice content, message IDs, contact names, filenames, cookies, API keys, tokens, or raw audio.</p><h2>Your controls</h2><p>You choose whether to transcribe each message. You can copy, retry, collapse, and change the language before retrying. You can clear local extension data through your browser’s extension storage controls. Uninstalling the extension removes its locally stored data according to your browser’s extension behavior.</p><h2>Security</h2><p>The Gemini API key is backend-only. VoiceText limits capture to the active target tab and releases recording resources after success, failure, cancellation, navigation, or tab closure. No system can guarantee absolute security, so keep the local backend and its configuration protected.</p><h2>Children</h2><p>VoiceText is not directed to children under 13, and we do not knowingly collect personal information from children.</p><h2>Changes to this policy</h2><p>We may update this policy when VoiceText changes. The “Last updated” date will identify the current version.</p><h2>Contact</h2><p>For privacy questions, open an issue in the VoiceText project repository.</p></article></main><VoiceTextFooter /></div>;
}
